'use client'
import type { Transaction, Distribution } from '@/types'
export function AdminTxnTabs({ pending, all, distributions, fees }: { pending: Transaction[], all: Transaction[], distributions: any[], fees: any[] }) { return <div>AdminTxnTabs</div> }
