import type { Investor, Property, Ownership } from '@/types'
export function CertificateTemplate({ investor, property, ownership }: { investor: Investor, property: Property, ownership: Ownership }) { return null }
